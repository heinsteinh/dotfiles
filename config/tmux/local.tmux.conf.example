# Local Tmux Configuration Template
# Copy this file to local.tmux.conf and customize as needed
# This file is sourced after the main .tmux.conf configuration

# Example local customizations:

# Custom key bindings
# bind-key -n C-h select-pane -L
# bind-key -n C-j select-pane -D
# bind-key -n C-k select-pane -U
# bind-key -n C-l select-pane -R

# Custom status bar configuration
# set -g status-left-length 50
# set -g status-right-length 100

# Window and pane specific settings
# set -g window-active-style 'bg=colour236,fg=colour255'
# set -g window-style 'bg=colour238,fg=colour245'

# Custom plugins (requires TPM)
# set -g @plugin 'tmux-plugins/tmux-cpu'
# set -g @plugin 'tmux-plugins/tmux-battery'

# Mouse settings override
# set -g mouse on

# Copy mode settings
# bind-key -T copy-mode-vi v send-keys -X begin-selection
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'pbcopy'

# Session management
# bind-key S choose-session

# Custom status bar elements
# set -g status-right "#(~/.config/tmux/scripts/cpu.sh) | #(~/.config/tmux/scripts/battery.sh) | %H:%M %d-%b-%y"

# Pane border colors
# set -g pane-border-style fg=colour238
# set -g pane-active-border-style fg=colour154

# Window settings
# setw -g automatic-rename on
# set -g renumber-windows on

# Environment specific settings
# if-shell "test -f ~/.tmux.local.conf" "source ~/.tmux.local.conf"
